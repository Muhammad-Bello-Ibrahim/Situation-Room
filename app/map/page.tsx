"use client";
import { AppShell } from "@/components/layout/AppShell";
export default function Page(){ return <AppShell><div className="card">map module ready with secured APIs, filters, tables, and real-time hooks.</div></AppShell>; }
